/**
 * This package contains the classes for the observer pattern.
 * The Observer will notify the Controller when the score inside the Model class is updated
 * It also contains the Observable interface which adds, removes and notifies observers when the score inside the Model is updated
 * @author Andreas Backman
 */

package com.dt181g.project.observer_pattern;